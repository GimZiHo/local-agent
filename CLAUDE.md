# CLAUDE.md

이 파일은 새 대화방에서도 동일한 방식으로 학습을 이어가기 위한 지침이다.
Claude는 세션 시작 시 이 파일과 `README.md`의 "학습 기록 / 다음 학습 주제"만 읽고 바로 작업을 시작한다.

## 1. 프로젝트 목적

- 로컬 LLM(Ollama + `qwen3.5:9b`)으로 AI Agent의 구조를 **직접 구현하며 학습**한다.
- 프레임워크(LangChain 등)에 의존하지 않고 원리를 손으로 만든다.
- 학습 결과를 GitHub에 축적해 **포트폴리오**로 만든다.

## 2. 실행 환경

- Windows 11 노트북에서 Ollama 실행, WSL(Ubuntu)의 Python에서 호출.
- Ollama 주소: `http://172.31.16.1:11434` (WSL에서 본 Windows 호스트 IP. 재부팅 시 바뀔 수 있음)
- 가상환경: `.venv` (git 제외)
- 모델: `qwen3.5:9b`

## 3. 학습 루프 (가장 중요)

**한 개념 = 코드 1개 + 학습 문서 1개 + 커밋 1개**

1. 이번에 공부할 개념 1개를 정한다 (README의 "다음 학습 주제" 참고).
2. 최소한의 코드로 구현한다. 동작 확인은 사용자가 직접 실행한다.
3. `docs/NN-주제.md` 학습 문서를 1개 작성한다 (아래 템플릿).
4. README의 "학습 기록" 표에 한 줄 추가하고, "다음 학습 주제"를 갱신한다.
5. 커밋한다. 한 커밋에 두 개념을 섞지 않는다.

## 4. 커밋 규칙

Conventional Commits + 한국어 본문.

```
<type>(<scope>): <한 줄 요약>

- 무엇을 구현했는지 1~3줄
- 배운 핵심 개념 1줄
```

- type: `feat`(기능) `fix`(버그) `docs`(문서) `refactor`(구조 개선) `chore`(설정/잡무)
- scope: `llm`, `memory`, `tool`, `agent`, `setup` 등 건드린 영역
- 예: `feat(tool): 파일을 읽는 read_file 툴 추가`

## 5. 학습 문서 템플릿

파일명: `docs/NN-주제.md` (NN은 두 자리 순번)

```markdown
# NN. 주제

## 배운 것
- 핵심 개념 3줄 이내

## 왜 필요한가
- 1~2줄

## 코드 핵심
```python
가장 중요한 5~15줄만
```

## 막혔던 점 / 해결
- 있으면 적고 없으면 생략

## 다음
- 이어서 공부할 것 1줄
```

분량은 **한 화면(50줄 이내)**. 길게 쓰지 않는다.

## 6. 토큰 절약 규칙

- 파일은 필요한 부분만 읽는다 (`sed -n`, `grep`). 전체 읽기 금지.
- 설명은 짧게. 이미 문서에 쓴 내용을 대화에서 반복하지 않는다.
- 코드는 동작하는 최소 예제로. 예외 처리, 타입, 추상화는 필요해지면 그때 추가한다.
- 리팩터링/기능 추가를 먼저 제안하지 않는다. 사용자가 요청한 개념 1개만 다룬다.
- 검색/웹 조회는 사용자가 요청할 때만.
- 세션 시작 시 코드 전체를 훑지 말고, 이번 주제에 관련된 파일만 본다.

## 7. 파일 구조

```text
local-agent/
├── CLAUDE.md          # 이 지침
├── README.md          # 프로젝트 소개 + 학습 기록 + 다음 주제
├── docs/              # 개념별 학습 문서
├── llm.py             # 메인 대화 루프
├── memory.py          # 장기 기억 저장/병합
├── memory_extract.py  # 대화에서 기억할 정보 추출
└── memory.json        # 실제 기억 데이터 (git 제외)
```
